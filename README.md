# varray-usages

```
# pre-requisites: linux, git, c compiler, janet, etc.
git clone https://github.com/sogaiu/varray-usages
cd varray-usages
./bin/pre-commit.janet

# browse usages/varray.janet
```

